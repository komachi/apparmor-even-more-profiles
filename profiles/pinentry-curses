abi <abi/3.0>,

include <tunables/global>

profile pinentry-curses /{usr/,}bin/pinentry-curses flags=(complain) {
  include <abstractions/base>

  /{usr/,}bin/pinentry-curses rix,

  include if exists <local/pinentry-curses>
} 
